# 📱 Запуск приложения через QR-код (Expo Go)

## 🚀 Сервер запущен!

Expo сервер запущен в фоновом режиме.

## 📱 Как открыть на iPhone:

### Способ 1: Через Expo Go (рекомендуется)

1. **Установите Expo Go** на iPhone из App Store (если еще не установлено)
2. **Откройте Expo Go** на iPhone
3. **Отсканируйте QR-код** из терминала:
   - В терминале появится QR-код
   - Откройте камеру iPhone или Expo Go
   - Наведите на QR-код
   - Нажмите на уведомление

### Способ 2: Через камеру iPhone

1. **Откройте приложение "Камера"** на iPhone
2. **Наведите на QR-код** из терминала
3. **Нажмите на уведомление**, которое появится
4. Приложение откроется в Expo Go

### Способ 3: Вручную через URL

1. **Скопируйте URL** из терминала (например: `exp://192.168.1.13:8081`)
2. **Откройте Expo Go** на iPhone
3. **Нажмите "Enter URL manually"**
4. **Вставьте URL** и нажмите "Connect"

## ⚠️ Важно:

- **iPhone и компьютер должны быть в одной Wi-Fi сети**
- **Expo Go должен быть установлен** на iPhone
- **Порт 8081 должен быть открыт** в файрволе

## 🔍 Если не работает:

1. **Проверьте Wi-Fi:**
   - Убедитесь, что iPhone и компьютер в одной сети
   - Проверьте, что Wi-Fi включен на обоих устройствах

2. **Проверьте файрвол:**
   - Разрешите доступ для Node.js в файрволе Windows
   - Порт 8081 должен быть открыт

3. **Попробуйте другой способ:**
   - Используйте туннель: `npx expo start --tunnel`
   - Или LAN: `npx expo start --lan`

4. **Перезапустите сервер:**
   - Нажмите `Ctrl+C` в терминале
   - Выполните: `npx expo start --clear`

## 📋 Полезные команды:

```bash
# Запуск с очисткой кеша
npx expo start --clear

# Запуск через туннель (работает из любой сети)
npx expo start --tunnel

# Запуск через LAN
npx expo start --lan

# Остановка сервера
# Нажмите Ctrl+C в терминале
```

## ✅ После подключения:

- Приложение загрузится в Expo Go
- Вы увидите изменения в реальном времени
- Можно разрабатывать и тестировать

---

**QR-код появится в терминале через несколько секунд!**

