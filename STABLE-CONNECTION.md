# 🔧 ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С ПЕРЕЗАПУСКОМ

## ❌ ПРОБЛЕМА:

Приложение открылось первый раз, но при сканировании второго QR-кода не открылось.

**Причина:** Сервер перезапустился и изменился URL/порт, или подключение разорвалось.

---

## ✅ РЕШЕНИЕ:

### Использован туннель для стабильного подключения

Туннель (`--tunnel`) создаёт постоянный URL, который не меняется при перезапуске.

---

## 📋 ИНСТРУКЦИЯ:

### ШАГ 1: Дождитесь запуска сервера

1. **Найдите окно PowerShell** с Expo сервером
2. **Дождитесь создания туннеля** (30-60 секунд)
3. **Когда появится запрос на авторизацию:**
   - Используйте стрелку **ВНИЗ ↓**
   - Выберите **"Proceed anonymously"**
   - Нажмите **Enter**

### ШАГ 2: Найдите QR-код

1. **Дождитесь QR-кода** в терминале
2. **URL будет вида:** `exp://u.expo.dev/...` (туннель)
3. **Этот URL не изменится** при перезапуске

### ШАГ 3: Откройте приложение

1. **Откройте Expo Go** на iPhone
2. **Нажмите "Scan QR code"**
3. **Отсканируйте QR-код** из терминала
4. **Приложение откроется**

---

## 🔄 ЕСЛИ НУЖНО ПЕРЕЗАПУСТИТЬ:

### Вариант 1: Используйте тот же QR-код

- **QR-код с туннелем не меняется**
- Просто отсканируйте его снова

### Вариант 2: Перезапустите сервер

```powershell
# Остановите сервер (Ctrl+C в окне PowerShell)
# Затем запустите снова:
npx expo start --tunnel --clear
```

---

## ⚠️ ВАЖНО:

- ✅ **Туннель создаёт стабильный URL** - он не меняется
- ✅ **Можно использовать один QR-код** многократно
- ✅ **Работает даже при смене Wi-Fi сети**
- ⚠️ **Туннель может быть медленнее** чем LAN (но стабильнее)

---

## 🚨 ЕСЛИ НЕ РАБОТАЕТ:

1. **Проверьте окно PowerShell** - есть ли ошибки?
2. **Дождитесь полного запуска** - туннель создаётся 30-60 секунд
3. **Попробуйте LAN режим:**
   ```powershell
   npx expo start --clear
   ```
   (но URL может измениться при перезапуске)

---

**Сервер запущен с туннелем! Дождитесь QR-кода и отсканируйте его! 🚀**

