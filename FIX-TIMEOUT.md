# 🔧 ИСПРАВЛЕНИЕ: "The request timed out"

## ❌ ПРОБЛЕМА:

Ошибка: **"The request timed out"** при попытке открыть приложение через Expo Go.

## ✅ РЕШЕНИЕ:

### Вариант 1: Использовать TUNNEL (Рекомендуется)

Tunnel обходит проблемы с файрволом и сетью:

```powershell
npx expo start --tunnel --clear
```

**Преимущества:**
- ✅ Работает даже если устройства в разных сетях
- ✅ Обходит файрвол автоматически
- ✅ Более стабильное подключение

**Недостатки:**
- ⏳ Медленнее чем LAN (30-60 секунд на запуск)
- 🌐 Требует интернет-соединение

---

### Вариант 2: Проверить файрвол Windows

1. Откройте **Панель управления** → **Брандмауэр Windows**
2. Нажмите **"Разрешить взаимодействие с приложением"**
3. Найдите **Node.js** в списке
4. Установите галочки для **Частная** и **Публичная** сети
5. Нажмите **ОК**

Или добавьте правило через PowerShell (от имени администратора):

```powershell
New-NetFirewallRule -DisplayName "Node.js Expo" -Direction Inbound -Protocol TCP -LocalPort 8081 -Action Allow
```

---

### Вариант 3: Использовать LAN с правильным IP

1. Узнайте IP-адрес компьютера:
   ```powershell
   ipconfig | findstr IPv4
   ```

2. Запустите сервер:
   ```powershell
   npx expo start --lan --clear
   ```

3. Используйте URL вида: `exp://192.168.x.x:8081`

---

## 🔍 ДИАГНОСТИКА:

### Проверка порта:
```powershell
netstat -ano | findstr :8081
```

Должна быть строка с `LISTENING`

### Проверка подключения с iPhone:
1. Убедитесь, что iPhone и компьютер в одной Wi-Fi сети
2. Проверьте, что Wi-Fi не изолирует устройства (гостевой режим)
3. Попробуйте отключить VPN на iPhone (если включен)

---

## 📱 ПОСЛЕ ЗАПУСКА ЧЕРЕЗ TUNNEL:

1. Дождитесь появления QR-кода в терминале (30-60 секунд)
2. Откройте **Expo Go** на iPhone
3. Отсканируйте QR-код
4. Приложение должно открыться без таймаута

---

## ⚠️ ВАЖНО:

- **Tunnel требует интернет** - убедитесь, что компьютер подключен к интернету
- **Tunnel медленнее** - но более надёжный
- Если tunnel не работает - проверьте интернет-соединение

---

**Проблема должна быть решена! 🚀**

