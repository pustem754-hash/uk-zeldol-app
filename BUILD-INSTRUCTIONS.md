# 🚀 ИНСТРУКЦИЯ: Сборка iOS приложения через EAS Build

## ✅ БЫСТРЫЙ СТАРТ:

### Вариант 1: Автоматический (рекомендуется)

```powershell
.\BUILD-iOS.ps1
```

Скрипт автоматически:
- Проверит установку EAS CLI
- Проверит авторизацию
- Запустит сборку

---

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ:

### ШАГ 1: Остановите все процессы Node.js

```powershell
taskkill /F /IM node.exe
```

Если выдаст ошибку "процесс не найден" - это нормально, продолжайте.

---

### ШАГ 2: Проверьте установку EAS CLI

```powershell
eas --version
```

**Если команда не найдена**, установите:

```powershell
npm install -g eas-cli
```

Проверьте снова:

```powershell
eas --version
```

Должна показать версию (например: `eas-cli/16.27.0`).

---

### ШАГ 3: Авторизуйтесь в Expo

```powershell
eas whoami
```

**Если показывает "Not logged in"**, выполните:

```powershell
eas login
```

Введите:
- **Email** от Expo аккаунта
- **Пароль**

После ввода проверьте снова:

```powershell
eas whoami
```

Должен показать имя пользователя.

---

### ШАГ 4: Запустите iOS сборку

```powershell
eas build --platform ios --profile preview --clear-cache
```

**Что произойдёт:**

1. Начнётся загрузка проекта на сервер Expo (~1-2 минуты)
2. Запустится облачная сборка (~10-20 минут)
3. В терминале появится ссылка вида: `https://expo.dev/accounts/[user]/projects/greenvalley/builds/[id]`
4. Появится текст: "Build in progress..." и прогресс
5. **НЕ закрывайте терминал!** Дождитесь сообщения: "Build finished"

---

### ШАГ 5: Получите ссылку на сборку

**После завершения сборки:**

1. **Скопируйте ссылку** из терминала (она будет вида: `https://expo.dev/accounts/.../builds/...`)
2. **Откройте ссылку** в Safari на iPhone
3. **Установите приложение** по инструкции в `INSTALL-INSTRUCTIONS.md`

**Альтернативно:**

1. Откройте браузер
2. Зайдите на https://expo.dev
3. Войдите в аккаунт
4. Перейдите в **"Builds"**
5. Найдите последнюю iOS сборку (preview)
6. Откройте её
7. Найдите кнопку **"Install"** или раздел с QR-кодом для установки
8. Скопируйте ссылку для установки

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ:

- ❌ **НЕ запускайте** `npx expo start` - это для разработки, не для сборки
- ❌ **НЕ используйте** Expo Go - это для тестирования в режиме разработки
- ✅ **Дождитесь завершения** `eas build` - это займёт 10-20 минут
- ✅ **Сохраните ссылку** на сборку - она понадобится для установки на iPhone

---

## 🔧 РЕШЕНИЕ ПРОБЛЕМ:

### Ошибка: "eas: command not found"

```powershell
npm install -g eas-cli
```

### Ошибка: "Not logged in"

```powershell
eas login
```

### Ошибка: "Project not configured"

Убедитесь что файлы `app.json` и `eas.json` существуют в корне проекта.

### Ошибка: "No Expo account"

1. Зайдите на https://expo.dev
2. Нажмите **"Sign Up"**
3. Создайте аккаунт
4. Вернитесь к команде `eas login`

---

## 📱 ПОСЛЕ СБОРКИ:

Следуйте инструкции в файле **`INSTALL-INSTRUCTIONS.md`** для установки на iPhone.

---

**Удачи! 🚀**

