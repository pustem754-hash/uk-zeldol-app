# 🔴 СРОЧНОЕ ИСПРАВЛЕНИЕ: Приложение подключается к dev server

## ❌ ПРОБЛЕМА ОБНАРУЖЕНА:

Приложение пытается подключиться к `exp://192.168.1.13:8081` - это означает, что оно собрано как **development build**, а не **standalone**.

## ✅ ЧТО ИСПРАВЛЕНО:

1. **Явно отключен expo-dev-client** в `eas.json`:
   ```json
   "preview": {
     "developmentClient": false,
     ...
   },
   "production": {
     "developmentClient": false,
     ...
   }
   ```

2. **Удален expo-dev-client** из зависимостей:
   - Выполнено: `npm uninstall expo-dev-client --legacy-peer-deps`
   - Пакет полностью удален из `package.json`

## 🚀 КРИТИЧЕСКИ ВАЖНО: ПЕРЕСБОРКА!

**После этих изменений ОБЯЗАТЕЛЬНО пересоберите приложение:**

```bash
eas build --platform ios --profile preview --clear-cache
```

**Флаг `--clear-cache` обязателен!**

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ:

### Шаг 1: Авторизация (если нужно)
```bash
eas login
```

### Шаг 2: Пересборка
```bash
eas build --platform ios --profile preview --clear-cache
```

### Шаг 3: Установка на iPhone

1. **Удалите старое приложение** полностью с iPhone
2. **Перезагрузите iPhone**
3. **Установите новую версию** через ссылку из EAS Build
4. **Доверьте разработчику** в настройках iPhone

## ✅ ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

После пересборки приложение должно:
- ✅ Работать **БЕЗ** подключения к серверу разработки
- ✅ Быть полностью **standalone** приложением
- ✅ **НЕ** показывать ошибку "Internet connection appears to be offline"
- ✅ Работать **автономно** на iPhone
- ✅ Отображать правильную иконку

## 🔍 ПРОВЕРКА:

После установки проверьте:
- Приложение открывается без ошибок?
- Нет сообщения о подключении к серверу?
- Все функции работают?

## ⚠️ ЕСЛИ ПРОБЛЕМА СОХРАНЯЕТСЯ:

1. Убедитесь, что `expo-dev-client` удален из `package.json`
2. Проверьте, что в `eas.json` указано `"developmentClient": false`
3. Попробуйте production профиль:
   ```bash
   eas build --platform ios --profile production --clear-cache
   ```

---

**Выполните пересборку СЕЙЧАС для применения исправлений!**

