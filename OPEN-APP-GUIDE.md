# 📱 КАК ОТКРЫТЬ ПРИЛОЖЕНИЕ НА iPHONE

## ✅ СПОСОБ 1: Через QR-код (Рекомендуется)

### Шаг 1: Установите Expo Go
- Откройте **App Store** на iPhone
- Найдите **"Expo Go"**
- Установите приложение

### Шаг 2: Запустите сервер
В терминале выполните:
```powershell
npx expo start --clear
```

### Шаг 3: Отсканируйте QR-код
1. Дождитесь появления **QR-кода** в терминале (10-20 секунд)
2. Откройте **Expo Go** на iPhone
3. Нажмите **"Scan QR code"**
4. Наведите камеру на QR-код из терминала
5. Приложение откроется автоматически

---

## ✅ СПОСОБ 2: Через URL вручную

### Если QR-код не работает:

1. В терминале найдите строку вида:
   ```
   exp://192.168.1.13:8081
   ```
   (IP-адрес может отличаться)

2. Откройте **Expo Go** на iPhone

3. Нажмите **"Enter URL manually"**

4. Введите URL из терминала (например: `exp://192.168.1.13:8081`)

5. Нажмите **"Connect"**

---

## ⚠️ ВАЖНО: Проверьте Wi-Fi

**iPhone и компьютер должны быть в одной Wi-Fi сети!**

1. Проверьте название Wi-Fi сети на iPhone
2. Проверьте название Wi-Fi сети на компьютере
3. Они должны совпадать!

---

## 🔧 ЕСЛИ НЕ РАБОТАЕТ

### Проблема: QR-код не сканируется
- Убедитесь, что iPhone и компьютер в одной Wi-Fi сети
- Попробуйте ввести URL вручную (способ 2)

### Проблема: "Unable to connect"
- Проверьте файрвол Windows - разрешите доступ для Node.js
- Убедитесь, что порт 8081 не заблокирован
- Попробуйте перезапустить сервер: `npx expo start --clear`

### Проблема: Сервер не запускается
- Остановите все процессы Node.js: `taskkill /F /IM node.exe`
- Запустите заново: `npx expo start --clear`

### Проблема: "Tunnel URL not found"
- Используйте LAN вместо tunnel: `npx expo start --lan`
- Или просто: `npx expo start --clear`

---

## 📋 БЫСТРЫЕ КОМАНДЫ

### Остановить сервер:
Нажмите `Ctrl+C` в терминале где запущен сервер

### Запустить сервер:
```powershell
npx expo start --clear
```

### Запустить через LAN:
```powershell
npx expo start --lan --clear
```

### Проверить статус порта:
```powershell
netstat -ano | findstr :8081
```

---

**Удачи! 🚀**

