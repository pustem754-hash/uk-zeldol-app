# 🔧 ИСПРАВЛЕНИЕ ПРОБЛЕМЫ С АВТОРИЗАЦИЕЙ

## ❌ ПРОБЛЕМА:

При запуске Expo появляется запрос:
```
It is recommended to log in with your Expo account before proceeding.
> Log in
  Proceed anonymously
```

И ошибка:
```
[redirect middleware]: Unable to determine redirect location for runtime 'custom' and platform 'ios'
```

---

## ✅ РЕШЕНИЕ:

### Способ 1: Выбрать "Proceed anonymously" (РЕКОМЕНДУЕТСЯ)

1. **В окне PowerShell с Expo сервером:**
   - Используйте стрелки **ВВЕРХ/ВНИЗ** на клавиатуре
   - Выберите **"Proceed anonymously"**
   - Нажмите **Enter**

2. **Дождитесь QR-кода** (10-20 секунд)

3. **Откройте приложение** через QR-код в Expo Go

---

### Способ 2: Войти в аккаунт (если есть)

1. Выберите **"Log in"**
2. Введите email и пароль
3. Дождитесь QR-кода

---

### Способ 3: Запуск без авторизации (через переменную окружения)

```powershell
$env:EXPO_NO_DOTENV = "1"
npx expo start --clear --offline
```

---

## 📋 ЧТО ДЕЛАТЬ СЕЙЧАС:

1. **Найдите окно PowerShell** с Expo сервером
2. **Выберите "Proceed anonymously"** (стрелки + Enter)
3. **Дождитесь QR-кода**
4. **Откройте приложение** через QR-код

---

## ⚠️ ВАЖНО:

- Ошибка `[redirect middleware]` не критична - приложение всё равно запустится
- Выбор "Proceed anonymously" безопасен для разработки
- После выбора QR-код появится через 10-20 секунд

---

**Выберите "Proceed anonymously" в окне PowerShell и дождитесь QR-кода! 🚀**

