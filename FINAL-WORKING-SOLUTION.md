# ✅ ФИНАЛЬНОЕ РЕШЕНИЕ: Максимально простая конфигурация

## 🔧 Что было сделано:

### 1. Упрощен `app.json` до минимума
- ✅ Убраны все лишние настройки (`runtimeVersion`, `extra`, `scheme`, `backgroundColor`)
- ✅ Оставлены только необходимые параметры для standalone приложения
- ✅ Правильно настроены плагины и разрешения
- ✅ Иконка указана в двух местах: общая и для iOS

### 2. Упрощен `eas.json` до минимума
- ✅ Убраны все сложные настройки (`developmentClient`, `resourceClass`, `buildConfiguration`)
- ✅ Оставлена только базовая конфигурация для preview и production
- ✅ `simulator: false` для preview (реальные устройства)

## 🚀 КОМАНДА ДЛЯ СБОРКИ:

```bash
eas build --platform ios --profile preview --clear-cache
```

## 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ:

### Шаг 1: Убедитесь, что вы авторизованы
```bash
eas whoami
```

Если не авторизованы:
```bash
eas login
```

### Шаг 2: Запустите сборку
```bash
eas build --platform ios --profile preview --clear-cache
```

**ВАЖНО:** Используйте флаг `--clear-cache` для полной очистки!

### Шаг 3: Дождитесь завершения сборки
- Сборка займет 10-20 минут
- Вы увидите прогресс в терминале
- После завершения получите QR-код и ссылку

### Шаг 4: Установка на iPhone

1. **УДАЛИТЕ старое приложение:**
   - Долгое нажатие на иконку
   - Выберите "Удалить приложение"
   - Подтвердите удаление

2. **ПЕРЕЗАГРУЗИТЕ iPhone:**
   - Нажмите и удерживайте кнопку питания
   - Выберите "Перезагрузить"

3. **УСТАНОВИТЕ новую версию:**
   - Откройте ссылку из EAS Build в Safari на iPhone
   - Или отсканируйте QR-код из терминала
   - Следуйте инструкциям установки

4. **ДОВЕРЬТЕ разработчику (если требуется):**
   - Настройки → Основные → VPN и управление устройством
   - Найдите профиль разработчика
   - Нажмите "Доверять"

### Шаг 5: Проверка

После установки проверьте:
- ✅ Иконка отображается на главном экране?
- ✅ Приложение открывается без вылетов?
- ✅ Все функции работают?

## ⚠️ ЕСЛИ ПРОБЛЕМА СОХРАНЯЕТСЯ:

### Вариант 1: Попробуйте production профиль
```bash
eas build --platform ios --profile production --clear-cache
```

### Вариант 2: Проверьте логи сборки
```bash
eas build:list
eas build:view
```

### Вариант 3: Проверьте сертификаты
```bash
eas credentials
```

### Вариант 4: Удалите expo-dev-client (если не нужен)
Если вы не используете expo-dev-client для разработки, можно удалить его:
```bash
npm uninstall expo-dev-client
```

Затем пересоберите:
```bash
eas build --platform ios --profile preview --clear-cache
```

## 🔍 ДИАГНОСТИКА:

### Если иконка не отображается:
1. Проверьте размер файла: должен быть 1024x1024 пикселей
2. Проверьте путь: `./assets/icon/app-icon.png`
3. Пересоберите с полной очисткой кеша

### Если приложение не открывается:
1. Проверьте логи устройства (Console.app на Mac)
2. Проверьте настройки доверия разработчику
3. Попробуйте production профиль вместо preview

## ✅ ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

После пересборки с этой простой конфигурацией приложение должно:
- ✅ Отображать иконку на главном экране iPhone
- ✅ Запускаться без вылетов и ошибок
- ✅ Работать как полноценное standalone приложение
- ✅ Иметь все необходимые разрешения

## 📞 ДОПОЛНИТЕЛЬНАЯ ПОМОЩЬ:

Если проблемы сохраняются:
- Проверьте логи на https://expo.dev
- Документация: https://docs.expo.dev/build/introduction/
- Поддержка: https://expo.dev/support

---

**Эта конфигурация максимально простая и должна работать для standalone iOS приложения!**

