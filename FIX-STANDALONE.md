# 🔴 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Приложение подключается к dev server

## ❌ ПРОБЛЕМА:

Приложение пытается подключиться к `exp://192.168.1.13:8081` - это означает, что оно собрано как development build, а не standalone.

## ✅ ЧТО ИСПРАВЛЕНО:

1. **Явно отключен expo-dev-client** в `eas.json`:
   - Добавлено `"developmentClient": false` для preview и production профилей

2. **Удален expo-dev-client** из зависимостей:
   - Выполнено: `npm uninstall expo-dev-client`

## 🚀 ПЕРЕСБОРКА ОБЯЗАТЕЛЬНА:

После этих изменений **ОБЯЗАТЕЛЬНО** пересоберите приложение:

```bash
eas build --platform ios --profile preview --clear-cache
```

## ⚠️ ВАЖНО:

1. **Удалите старое приложение** с iPhone полностью
2. **Перезагрузите iPhone**
3. **Установите новую версию** после пересборки
4. **Проверьте**, что приложение работает без подключения к серверу

## ✅ ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:

После пересборки приложение должно:
- ✅ Работать **БЕЗ** подключения к серверу разработки
- ✅ Быть полностью **standalone** приложением
- ✅ Не показывать ошибку "Internet connection appears to be offline"
- ✅ Работать автономно на iPhone

## 🔍 ЕСЛИ ПРОБЛЕМА СОХРАНЯЕТСЯ:

Проверьте, что в `eas.json` для preview и production указано:
```json
"developmentClient": false
```

И убедитесь, что `expo-dev-client` удален из `package.json`.

---

**Выполните пересборку с --clear-cache для применения изменений!**

