# 🔴 ИСПРАВЛЕНИЕ: QR-код не открывается

## ✅ ЧТО СДЕЛАНО:

1. **Сервер перезапущен через туннель** (`--tunnel`)
   - Работает из любой сети
   - Не требует настройки файрвола
   - Более надежное подключение

2. **Очищен кеш** (`--clear`)

## 📱 КАК ОТКРЫТЬ НА iPhone:

### Шаг 1: Установите Expo Go
- Откройте **App Store** на iPhone
- Найдите **"Expo Go"**
- Установите приложение

### Шаг 2: Откройте Expo Go
- Запустите приложение **Expo Go** на iPhone

### Шаг 3: Отсканируйте QR-код
- В терминале появится **QR-код** (через 10-20 секунд)
- В Expo Go нажмите **"Scan QR code"**
- Наведите камеру на QR-код из терминала
- Или используйте камеру iPhone → наведите на QR-код → нажмите на уведомление

## 🔍 ЕСЛИ QR-КОД НЕ РАБОТАЕТ:

### Вариант 1: Введите URL вручную
1. В терминале найдите строку типа: `exp://xxx.xxx.xxx.xxx:8081`
2. Скопируйте этот URL
3. В Expo Go нажмите **"Enter URL manually"**
4. Вставьте URL и нажмите **"Connect"**

### Вариант 2: Используйте туннель
Сервер уже запущен через туннель. QR-код должен работать.

### Вариант 3: Проверьте интернет
- Убедитесь, что iPhone подключен к интернету
- Туннель требует интернет-соединение

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ:

### Проблема: "Unable to connect to server"
**Решение:**
- Убедитесь, что сервер запущен в терминале
- Проверьте интернет-соединение
- Попробуйте перезапустить сервер: `npx expo start --tunnel --clear`

### Проблема: QR-код не появляется
**Решение:**
- Подождите 10-20 секунд
- Проверьте, что сервер запущен (должны быть логи в терминале)
- Попробуйте перезапустить: `npx expo start --tunnel --clear`

### Проблема: "Metro bundler error"
**Решение:**
- Остановите сервер (Ctrl+C)
- Выполните: `npx expo start --tunnel --clear`
- Подождите полной загрузки

## 🚀 АЛЬТЕРНАТИВНЫЕ КОМАНДЫ:

```bash
# Через туннель (работает из любой сети)
npx expo start --tunnel --clear

# Через LAN (требует одну Wi-Fi сеть)
npx expo start --lan --clear

# Обычный режим
npx expo start --clear
```

## 📋 ПРОВЕРКА:

1. **Сервер запущен?** - Должны быть логи в терминале
2. **QR-код появился?** - Должен быть в терминале через 10-20 секунд
3. **Expo Go установлен?** - Проверьте на iPhone
4. **Интернет работает?** - Проверьте на iPhone

---

**Сервер запущен через туннель. QR-код появится в терминале через несколько секунд!**

