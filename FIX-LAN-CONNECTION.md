# ✅ ИСПРАВЛЕНИЕ: Запуск через LAN (без туннеля)

## 🔧 ЧТО ИЗМЕНЕНО:

Туннель требует авторизации. Запущен сервер через **LAN** - работает в локальной сети.

## 📱 КАК ОТКРЫТЬ НА iPhone:

### Шаг 1: Убедитесь, что iPhone и компьютер в одной Wi-Fi сети
- **Важно!** Оба устройства должны быть подключены к **одной Wi-Fi сети**

### Шаг 2: Установите Expo Go (если еще не установлено)
- Откройте **App Store** на iPhone
- Найдите **"Expo Go"**
- Установите

### Шаг 3: Откройте Expo Go
- Запустите приложение **Expo Go** на iPhone

### Шаг 4: Отсканируйте QR-код
- В терминале появится **QR-код** (через 10-20 секунд)
- В Expo Go нажмите **"Scan QR code"**
- Наведите камеру на QR-код из терминала
- Или используйте камеру iPhone → наведите на QR-код → нажмите на уведомление

## 🔍 ЕСЛИ НЕ РАБОТАЕТ:

### Проблема: "Unable to connect"
**Решение:**
1. Убедитесь, что iPhone и компьютер в **одной Wi-Fi сети**
2. Проверьте файрвол Windows - разрешите доступ для Node.js
3. Попробуйте ввести URL вручную (см. ниже)

### Ввод URL вручную:
1. В терминале найдите строку типа: `exp://192.168.x.x:8081`
2. Скопируйте этот URL
3. В Expo Go нажмите **"Enter URL manually"**
4. Вставьте URL и нажмите **"Connect"**

### Альтернатива: Используйте обычный режим
```bash
npx expo start --clear
```

## ⚠️ ВАЖНО:

- **Одна Wi-Fi сеть** - критически важно!
- **Файрвол** - разрешите доступ для Node.js на порт 8081
- **QR-код появится** через 10-20 секунд после запуска

## 🚀 КОМАНДЫ:

```bash
# Через LAN (текущий режим)
npx expo start --lan --clear

# Обычный режим
npx expo start --clear

# С туннелем (требует авторизацию)
npx expo start --tunnel --clear
```

---

**Сервер запущен через LAN. Убедитесь, что iPhone и компьютер в одной Wi-Fi сети!**

